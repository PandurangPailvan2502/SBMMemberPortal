@model SBMMember.Web.Models.LoginViewModel
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutLogin.cshtml";
}
<script>


        $(document).ready(function () {

            $('#mobileNumber').keypress(function (e) {

                var charCode = (e.which) ? e.which : event.keyCode

                if (String.fromCharCode(charCode).match(/[^0-9]/g))

                    return false;

            });

            //$("#btnSendOtp").click(function () {
            //    alert($("#mobileNumber").val());
            //    var dataToPost = "{ mobile:'" + $("#mobileNumber").val() + "'}";
            //    $.ajax({
            //        type: "POST",
            //        url: "/OTPVerify/SendOTP",
            //        data: dataToPost,
            //        contentType: "application/json; charset=utf-8",
            //        dataType: "json",
            //        success: function (response) {
            //           // alert("Hello: " + response.Name + " .\nCurrent Date and Time: " + response.DateTime);
            //        },
            //        failure: function (response) {
            //            alert(response.responseText);
            //        },
            //        error: function (response) {
            //            alert(response.responseText);
            //        }
            //    });
            //});

            @*$("#btnSendOtp").click(function () {

                var mobileNum = $("#mobileNumber").val();
                var validateMobNum = /^\d*(?:\.\d{1,2})?$/;
                if (mobileNum.length == 10)
                {
                    //alert("Valid Mobile Number");

                    var url = '@Url.Action("SendOTP", "OTPVerify")';
                    var data = { mobile: mobileNum };

                $.post(url, data, function (result) {
                    // do something
                    if (result.status == "success") {
                        window.location = '@Url.Action("VerifyOTP", "OTPVerify")';
                    }
                });
                }
                else {
                    //alert("Invalid Mobile Number");
                    $("#lblError").text("Invalid Mobile Number");
                }
            });*@
        });//document close

</script>
<section class="blog_area single-post-area section_padding">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 posts-list">
                <div class="single-post">
                    <div class="blog_details">
                        <p class="excert">
                            To proceed with the one time registration first we will verify your mobile number
                        </p>
                        <div class="login-icon">
                            <img src="img/login-icon.png" width="110px" height="110px">
                        </div>
                    </div>
                </div>


                <div class="login-type">
                    <div class="form-group">
                        <div class="row"><p id="lblError" style="color:red;margin-left:40px;"></p></div>
                        <form method="post" enctype="multipart/form-data" asp-controller="OTPVerify" asp-action="SendOTP">
                            <div class="input-group mb-3">

                                <input id="mobileNumber" type="text" name="mobile" class="form-control myinputlogin" maxlength="10" placeholder='Enter Mobile No.' onfocus="this.placeholder = ''" onblur="this.placeholder = 'Enter Mobile No.'">
                            </div>

                            <div class="row">
                                <div class="col-md-6 d-none d-md-block d-sm-none d-lg-block">
                                    <button class="btn btn_12" id="btnSendOtp" type="submit" text="Send OTP">Send OTP</button>
                                </div>
                                <div class="col-md-6 d-none d-md-block d-sm-none d-lg-none">
                                    <button class="btn btn_121" type="button">Resend OTP</button>
                                    <p style="text-align:right">Didn’t Receive Yet?</p>
                                </div>

                                <div class="col-sm-6 d-none d-md-none d-sm-block d-xs-none">
                                    <button class="btn btn_12" type="button" text="Send OTP">Send OTP</button>
                                </div>
                                <div class="col-sm-6 d-none d-md-none d-sm-block d-xs-none">
                                    <button class="btn btn_121" type="button">Resend OTP</button>
                                    <p style="text-align:right">Didn’t Receive Yet?</p>
                                </div>

                                <div class="col-xs-6 d-none d-sm-none d-md-none">
                                    <button class="btn btn_12 " type="button" text="Send OTP">Send OTP</button>
                                </div>
                                <div class="col-xs-6 d-none d-sm-none d-md-none">
                                    <button class="btn btn_121" type="button">Resend OTP</button>
                                    <p style="text-align:right">Didn’t Receive Yet?</p>
                                </div>
                            </div>

                        </form>
                    </div>

                </div>

                <div class="navigation-top">

                </div>




            </div>

        </div>
    </div>
</section>

